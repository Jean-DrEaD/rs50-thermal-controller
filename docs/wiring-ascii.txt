══════════════════════════════════════════════════════════════════════════
  RS50 THERMAL CONTROLLER — WIRING DIAGRAM (TEXT VERSION)
  Version: 3.3.8 — CORRIGIDO 2026-05-12
══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                          POWER DISTRIBUTION                              │
└─────────────────────────────────────────────────────────────────────────┘

  XDrive Mini PSU ──┬── 24V ──► Songle Relay COM
   (24V/cabos       │            └─► NC ──► ODESC FFBeast (motor driver)
    1.5mm² fios)    │
                    ├── 24V ──► LM2596 #1 ─► 12V ──┬── Fan 12V (PWM direto)
                    │                               │
                    │                               └── LM2596 #2 ─► 5V ──► ESP32-S3-Zero VBUS
                    │
                    └── 24V ──► Bobina do relé (A1)

┌─────────────────────────────────────────────────────────────────────────┐
│                        ESP32-S3-ZERO PINOUT ✅                            │
└─────────────────────────────────────────────────────────────────────────┘

                     ┌──────────────────────┐
                     │   ESP32-S3-Zero      │
                     │                      │
   NTC ────────────► │ GPIO 1  (ADC1_CH0)   │
                     │                      │
   Fan PWM ─────────► │ GPIO 5  (25kHz)      │  ← PWM direto (sem MOSFET!)
                     │                      │
   Relé MOSFET ─────► │ GPIO 4               │ ──► IRLZ44N Gate (220Ω)
                     │                      │
   WS2812 ──────────► │ GPIO 9 + 330Ω        │  ← ✅ CORRIGIDO (não GPIO21!)
                     │                      │
                     │ GND ─────── GND      │
                     │ 5V  ─────── VBUS     │
                     └──────────────────────┘

⚠️ NÃO usar GPIO21: LED RGB onboard do ESP32-S3-Zero

┌─────────────────────────────────────────────────────────────────────────┐
│                          NTC THERMISTOR                                  │
└─────────────────────────────────────────────────────────────────────────┘

     3.3V ──[100kΩ 1%]──┬──► GPIO 1 (ESP32 ADC)
                        │
                        │
                     ┌──┴──┐
                     │ NTC │  100K B3950 (no estator)
                     │     │
                     └──┬──┘
                        │
                       GND
     (100nF entre GPIO1 e GND)

┌─────────────────────────────────────────────────────────────────────────┐
│                         FAN PWM (12V) — DIRETO                           │
└─────────────────────────────────────────────────────────────────────────┘

                  12V (LM2596 #1)
                    │
              ┌─────▼─────┐
              │  FAN 120mm│  ← 4-pin PWM nativo
              │ 12V PWM   │     (GPIO5 3.3V direto!)
              └─────┬─────┘
                    │
                    └─► Fan GND
GPIO 5 ────────────────► Fan PWM (25kHz, 3.3V logic)

┌─────────────────────────────────────────────────────────────────────────┐
│                  SAFETY RELAY (FAIL-SAFE NC) — IRLZ44N                   │
└─────────────────────────────────────────────────────────────────────────┘

⚠️ ATENÇÃO: Relé NC! ESP travar = motor continua (fail-safe)

                  24V
                   │
                   ▼
              ┌─────────┐
              │ Relé    │ COM ───► 24V fonte
              │ Songle  │
              │ SLA-24V │ NC  ───► ODESC VIN+ (motor power)
              │  30A    │
              └────┬────┘
                   │
                   │ Bobina 24V
                   │ A1 = +24V
                   │ A2 = Drain IRLZ44N
                   ▼
                [1N4007]  ← Flyback: K→A1 | A→A2
                   │
              ┌──┴────────┐
              │ IRLZ44N   │
              │ G ─[220Ω]► GPIO 4
              │  │[10k↓]  │  ← Pull-down
              │ S ────► GND
              └───────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          WS2812 STATUS LEDs                              │
└─────────────────────────────────────────────────────────────────────────┘

  5V ───────► WS2812 VCC
  GND ──────► WS2812 GND
GPIO 9 ─[330Ω]► WS2812 DIN  ← ✅ CORRIGIDO!

LED_COUNT: 1-5 LEDs (config.h)

┌─────────────────────────────────────────────────────────────────────────┐
│                       CABLE SPECIFICATIONS                               │
└─────────────────────────────────────────────────────────────────────────┘

► 24V (Relé/ODESC): 1.5mm² (~16AWG)
► NTC: 0.5mm² trançado (<30cm)
► WS2812: 0.5mm² (<50cm)
► Relé bobina: 0.5mm² OK

══════════════════════════════════════════════════════════════════════════
  Para diagrama gráfico: docs/wiring-schematic.svg (Fritzing)
══════════════════════════════════════════════════════════════════════════
